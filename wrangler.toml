# Used by Wrangler when deploying Pages Functions (functions/metrics.js).
# Deploy: npm run build && npx wrangler pages deploy dist --project-name=portfolio-frontend
compatibility_date = "2024-09-23"
